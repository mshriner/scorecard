import { TotalPuttsPipe } from './total-putts.pipe';

describe('TotalPuttsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPuttsPipe();
    expect(pipe).toBeTruthy();
  });
});
