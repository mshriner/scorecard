import { TotalRoundScorePipe } from './total-round-score.pipe';

describe('TotalRoundScorePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalRoundScorePipe();
    expect(pipe).toBeTruthy();
  });
});
